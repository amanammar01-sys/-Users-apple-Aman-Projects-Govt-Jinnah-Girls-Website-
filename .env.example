# Admin dashboard credentials (required on Vercel)
ADMIN_PASSWORD=your-secure-password
ADMIN_SECRET=change-this-to-a-long-random-secret-string

# Vercel Blob — required for admin saves & gallery uploads on Vercel
# Create a Blob store in Vercel: Project → Storage → Connect Blob
# Vercel adds BLOB_READ_WRITE_TOKEN automatically when connected
